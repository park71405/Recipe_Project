<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.recipe.mappers.memberMapper">

	<!-- 회원가입 -->
	<insert id="signup">
		insert into user(user_id, user_pw, user_name, user_phone, user_email) 
			values (#{user_id}, #{user_pw}, #{user_name}, #{user_phone}, #{user_email})
	</insert>

	<!-- 로그인 -->
	<select id="signin" resultType="com.recipe.domain.MemberVO">
		select
			user_id, user_pw, user_name, user_phone, user_email
		from
			user
		where user_id = #{user_id} 
	</select>

</mapper>
